import 'package:flutter/material.dart';

class Computation extends StatelessWidget {
  const Computation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:Placeholder(),
    );
  }
}
